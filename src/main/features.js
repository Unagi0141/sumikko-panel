/**
 * 配布版に載せる機能の切り替え。
 *
 * ログイン情報の保存は、自分ひとりで使うぶんには便利でも、他人に配ると
 * 意味が変わる。暗号化していても、事故ったときの責任は配った側に向く。
 * そのうえ X はログインの自動化にエラーを返すことが実測で分かっており、
 * この機能を同梱すると規約上いちばん摩擦の大きい部分を抱えることになる。
 *
 * そこで既定では丸ごと無効にし、機能そのもの（保存も UI も IPC も）を
 * 積まない。自分の環境で使いたいときだけ環境変数で有効にする。
 *
 *   TLDOCK_CREDENTIALS=1 npm start
 *
 * セッションは一度ログインすれば数か月維持されるので、無効でも実用上ほとんど困らない。
 */

const credentials = process.env.TLDOCK_CREDENTIALS === '1';

module.exports = {
  credentials,
};
